{
    "name": "cloud-app",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "heroku-postbuild": "cd client && npm install && npm run build"
    },
    "dependencies": {
        "react-paypal-button-v2": "^2.6.3"
    }
}
