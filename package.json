{
    "name": "cloud-app",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "heroku-postbuild": "cd client && npm install --legacy-peer-deps && npm run build"
    },
    "dependencies": {
        "express": "^4.21.2",
        "react": "^17.0.2",
        "react-dom": "^17.0.2"
    },
    "engines": {
        "node": "18.x",
        "npm": "10.x"
    }
}