{
    "name": "cloud-app",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "heroku-postbuild": "cd client && npm install --legacy-peer-deps && npm run build"
    },
    "dependencies": {
        "react": "^18.0.0",
        "react-dom": "^18.0.0",
        "react-paypal-button-v2": "^2.6.3"
    },
    "engines": {
        "node": "18.x",
        "npm": "10.x"
    }
}